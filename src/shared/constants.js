const CONSTANTS = {
    ALLOWED_ORIGINS: [
        'http://localhost:4200', // Local frontend development URL (e.g. Angular)
    ],
    ALLOWED_METHODS: 'GET, POST, PUT, DELETE, OPTIONS',
    ALLOWED_HEADERS: 'Content-Type, Authorization',
    ALLOWED_REGISTRATION_DOMAINS: ['com', 'net', 'org', 'edu'],
    // You can add more....
}

export default CONSTANTS